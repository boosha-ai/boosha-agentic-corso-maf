# Chiave OpenAI (provider cloud di default per le puntate)
OPENAI_API_KEY=sk-...

# Ollama gira in locale, non serve chiave (default http://localhost:11434)
# OLLAMA_HOST=http://localhost:11434

# Notifiche push (opzionali, da EP 1 in poi)
PUSHOVER_TOKEN=your_pushover_token
PUSHOVER_USER=your_pushover_user
